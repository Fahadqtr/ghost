# زر «حفظ الإجازة» بالماكرو (اختياري)

الملف الأساسي `جدول_الورديات_وحجز_الإجازات.xlsx` يسجّل الإجازة فورياً بمجرد الكتابة في
أي صف فارغ بجدول «حجز الإجازات» (جدول إكسل ذكي يحسب الأيام ويفحص التعارض تلقائياً).

هذا الدليل لمن يريد **زراً فعلياً**: تملأ منطقة «⚡ إدخال سريع» (الأعمدة L/M في ورقة
«حجز الإجازات») ثم تضغط الزر فيُضاف الصف ويُفرّغ الإدخال.

> ⚠️ ملاحظة مهمة: الماكرو يحتاج حفظ الملف بصيغة `.xlsm`، وكثير من أجهزة العمل
> الحكومية **تمنع الماكرو** بسياسة أمنية. إن كان ممنوعاً عندك، استمر باستخدام
> الإدخال المباشر في الجدول (يعمل بدون ماكرو ويسجّل فوراً).

## خانات الإدخال الجاهزة (ورقة «حجز الإجازات»)
| الخانة | المحتوى |
|---|---|
| M3 | الاسم (قائمة منسدلة) |
| M4 | نوع الإجازة (قائمة منسدلة) |
| M5 | من تاريخ |
| M6 | إلى تاريخ |
| M7 | الحالة (قائمة منسدلة) |

## خطوات التركيب (دقيقتان)
1. افتح الملف ← **File ← Save As** ← اختر النوع **Excel Macro-Enabled Workbook (\*.xlsm)** واحفظ.
2. اضغط **Alt + F11** (محرّر VBA) ← قائمة **Insert ← Module** ← الصق الكود بالأسفل.
3. أغلق المحرّر، ومن تبويب **Developer ← Insert ← Button (Form Control)** ارسم زراً في ورقة «حجز الإجازات».
   - إن لم يظهر تبويب Developer: **File ← Options ← Customize Ribbon** ← فعّل **Developer**.
4. عند رسم الزر يطلب تعيين ماكرو ← اختر **AddLeave** ← موافق. غيّر اسم الزر إلى «حفظ الإجازة».
5. جرّب: املأ M3..M7 واضغط الزر.

## كود الماكرو
```vba
Sub AddLeave()
    Dim ws As Worksheet
    Set ws = ThisWorkbook.Sheets("حجز الإجازات")

    Dim nm As String, ty As String, st As String
    Dim dFrom As Variant, dTo As Variant
    nm = Trim(ws.Range("M3").Value)
    ty = Trim(ws.Range("M4").Value)
    dFrom = ws.Range("M5").Value
    dTo = ws.Range("M6").Value
    st = Trim(ws.Range("M7").Value)

    ' تحقق أساسي
    If nm = "" Or dFrom = "" Or dTo = "" Then
        MsgBox "الرجاء تعبئة الاسم وتاريخ البداية والنهاية على الأقل.", vbExclamation
        Exit Sub
    End If
    If IsDate(dFrom) = False Or IsDate(dTo) = False Then
        MsgBox "تأكد من صحة التواريخ.", vbExclamation
        Exit Sub
    End If
    If CDate(dTo) < CDate(dFrom) Then
        MsgBox "تاريخ النهاية قبل البداية!", vbExclamation
        Exit Sub
    End If
    If st = "" Then st = "قيد الانتظار"

    Dim r As Long
    ws.Unprotect            ' إن كانت هناك كلمة مرور: ws.Unprotect "كلمتك"
    r = ws.Cells(ws.Rows.Count, "B").End(xlUp).Row + 1
    If r < 5 Then r = 5

    ws.Cells(r, 2).Value = nm        ' B: الاسم
    ws.Cells(r, 3).Value = ty        ' C: النوع
    ws.Cells(r, 4).Value = CDate(dFrom)  ' D: من
    ws.Cells(r, 5).Value = CDate(dTo)    ' E: إلى
    ws.Cells(r, 7).Value = st        ' G: الحالة
    ' الأعمدة F/H/I (الأيام/التعارض/التغطية) تُحسب تلقائياً بالصيغ

    ws.Range("M3,M4,M5,M6,M7").ClearContents   ' تفريغ الإدخال
    ws.Protect              ' إعادة الحماية

    ' تنبيه لو فيه تعارض
    If InStr(ws.Cells(r, 8).Value, "تعارض") > 0 Then
        MsgBox "تم التسجيل في الصف " & r & " ⚠ لكن هناك تعارض — راجع الطلب.", vbExclamation
    Else
        MsgBox "تم تسجيل الإجازة في الصف " & r & " ✓", vbInformation
    End If
End Sub
```

## ملاحظات
- الزر يعمل حتى مع حماية الورقة (الماكرو يلغي الحماية مؤقتاً ثم يعيدها).
- إن وضعت كلمة مرور للحماية، عدّل سطري `Unprotect` و`Protect` ليحتويا الكلمة.
- الجدول يتسع تلقائياً، وأعمدة الحساب جاهزة حتى الصف 104.
