# 📒 سجل بناء Malak AI — من الصفر إلى الآن

> توثيق كامل ومفصّل لكل ما بنيناه في مساعد **ملاك (Malak AI)** داخل مشروع `Fahadqtr/ghost`
> (متجر Malika's Universe Trading — جمال وكورية، قطر).
> الفرع: `master` · المسار: `commerce-ai-os/` · آخر تحديث: 2026-06-08

---

## 🎯 الفكرة العامة

**ملاك** = مديرة ذكية بصوت عربي خليجي تدير الكتالوج عبر صفحة `/malak`. تقرأ بيانات المنتجات،
تنفّذ تعديلات **بتأكيد إجباري**، تولّد صور وبوستات إعلانية، وتعطي بريفينج صباحي.
تدير فريق من **8 وكلاء متخصصين** + ملاك نفسها:

| الوكيل | القسم | الوكيل | القسم |
|---|---|---|---|
| **ملاك** | الإدارة العامة | **رزان** | التسعير |
| **نور** | الكتالوج | **راشد** | التقارير |
| **بيان** | المحتوى | **لطيفة** | العملاء |
| **ريم** | الصور | **سالم** | العمليات |
| **سراج** | التواصل والنشر | | |

---

## 🧱 التقنيات المستخدمة

- **Next.js 14** (App Router) + **TypeScript** + **Tailwind** + واجهة **عربية RTL**.
- **Anthropic Claude** (`claude-sonnet-4-6`) — حلقة استدعاء أدوات (tool-use loop).
- **Supabase** — قاعدة البيانات (service-role admin يتجاوز RLS للقراءة/الكتابة الإدارية).
- **ElevenLabs TTS** (`eleven_multilingual_v2`) — صوت لكل وكيل + Web Audio API للتشغيل.
- **OpenAI Images** (`gpt-image-1-mini`) — توليد صور إعلانية.
- **HMAC-SHA256** — توكنات موقّعة (صلاحية 15 دقيقة) لتأكيد أي عملية كتابة.

---

## 🗂️ خريطة الملفات

| الملف | الوظيفة |
|---|---|
| `app/malak/page.tsx` | غلاف خادمي (`force-dynamic`) يعرض الواجهة |
| `app/malak/MalakClient.tsx` | كل الواجهة: الأورب، اللوحات، البريفينج، زر الاستماع |
| `app/malak/error.tsx` | شبكة أمان: إعادة تحميل تلقائية عند خطأ chunk |
| `app/malak/layout.tsx` | غلاف RTL + خط Tajawal |
| `app/api/malak/route.ts` | **العقل**: البرومبت، الأدوات، موجّه الأدوات، حلقة الأدوات |
| `app/api/malak/commit/route.ts` | **المنفّذ الوحيد للكتابة** (HMAC) + سجل تدقيق + منع تكرار |
| `app/api/malak/generate-image/route.ts` | توليد صورة عبر OpenAI ورفعها لـStorage |
| `app/api/malak/speak/route.ts` | النطق (ElevenLabs) — صوت لكل وكيل + نطق الأرقام |
| `app/api/malak/upload/route.ts` | رفع صورة مرفقة لـStorage (بدون ربط) |
| `app/api/malak/briefing/route.ts` | البريفينج الصباحي (قراءة فقط) |
| `lib/malak/confirm.ts` | توقيع/تحقّق توكن HMAC |
| `lib/malak/intent.ts` | موجّه النية `detectForcedTool` |
| `scripts/export_talabat.mjs` | تصدير كتالوج طلبات (سكربت مستقل) |

---

## 📜 المراحل بالترتيب الزمني (حسب سجل الـcommits)

### المرحلة 1 — الأساس: ملاك تتكلم وتقرأ
- `9f07199` إنشاء Malak AI: مدير صوتي/بصري على `/malak`.
- `0b10444` صوت ElevenLabs حقيقي + fallback لصوت المتصفح.
- `73ad268` إصلاح ردود "تم." الفارغة (كان MAX_TOKENS منخفض) + تخطيط الجوال.
- **العقل (`route.ts`):** يحمل كل الأسرار، يشغّل حلقة أدوات، Claude يطلب بيانات
  الكتالوج ويرجّع `{ agent, speak, panel }` عبر أداة `respond`.
- **أدوات القراءة (5):** `search_products` · `catalog_stats` · `list_rejected`
  · `list_missing_images` · `low_stock`.

### المرحلة 2 — ضبط الصوت واللهجة
- `461fa91`/`63d22a5` لهجة خليجية أقوى + أمثلة نبرة + صوت أثبت بدون تأتأة.
- `89aca30`/`5bc7604` تشغيل الصوت عبر **Web Audio API** (حل مشكلة منع التشغيل
  التلقائي على الكمبيوتر) + منع الرجوع لنطق إنجليزي.
- `06bd0cb`/`47e05e8` **صوت مستقل لكل وكيل** + الدور تحت الاسم في الواجهة.

### المرحلة 2B — الكتابة بتأكيد إجباري (أهم مرحلة أمنيًا)
- `16776ed` أدوات كتابة + **تأكيد إجباري** + سجل تدقيق.
- `6a66ffd`/`3914ce2` منع الموديل من ادّعاء أن أدوات الكتابة "غير متاحة" + إجبار الأداة.
- **أدوات الكتابة (6):** `update_stock`(سالم) · `set_price`(رزان) · `set_approval`(نور)
  · `add_product`(نور+بيان) · `set_image`(ريم) · `generate_product_image`(ريم).
- **القاعدة الذهبية:** أي أداة كتابة **لا تكتب فورًا** — ترجّع كرت تأكيد + توكن HMAC موقّع،
  والكتابة الفعلية تحصل **فقط** في `/api/malak/commit` بعد ضغط المستخدم [أكّد].

### المرحلة 2C — رفع الصور
- `9c0c59b`/`12b49de` ريم ترفع صور المنتجات (بتأكيد) + أداة `list_missing_images`.

### المرحلة 2D — توليد الصور الإعلانية
- `1a4a8e6` ريم تولّد صورة إعلانية عبر OpenAI (تأكيد مزدوج، نفس آلية الأمان).
- `b07864c` مقاس انستجرام عمودي.
- `e3c36e0` **توليد صور بدون نص** (لأن الـAI يخربط النص العربي مثل PDRN→PORN) + ملاحظة
  معاينة: "أضف النص العربي بمحرر خارجي".

### موجّه الأدوات القسري (Tool Router)
- `d305c77` إصلاح "هروب" الأدوات: `detectForcedTool` يكتشف النية **قبل** Claude ويجبر الأداة
  المطابقة عبر `tool_choice` — الموديل ما يقدر يرد بنص بدل تنفيذ الأداة.
- شبكة أمان: لو أُجبرت أداة ولم تُستدعَ → رسالة محكومة بدل هلوسة.

### إصلاحات سجل التدقيق (`malak_audit`)
- `fa2f684` إصلاح: كتابة عمود `action` القديم (كان NOT NULL وغير مستخدم).
- `bddb298` إصلاح: التوقّف عن كتابة UUID في عمود `product_id` من نوع bigint
  (نخزّن productId داخل `details` بدلًا منه).
- `ccc7ce8` إظهار فشل التدقيق في الواجهة + **منع التنفيذ المزدوج** (idempotency، نافذة 30 ثانية).

### معالجة الكاش والأخطاء
- `f5d8e36` جعل `/malak` ديناميكية (`force-dynamic` + `no-store`) لإنهاء تسليم نسخ قديمة.
- `fe1bf04`/`8093d49` Error Boundary داخل الواجهة يعيد التحميل تلقائيًا عند خطأ chunk قديم.

### الشخصية والبوستات
- `e4c28c6` شخصية ملاك = شريكة أعمال صريحة + تنبيهات على الأرقام الشاذّة.
- `6ee217a` قالب بوست إعلاني احترافي لبيان (هوك/فوائد/مكونات/CTA + إنجليزي + 10 هاشتاقات).

### البريفينج الصباحي
- `25af744` بريفينج راشد التلقائي عند فتح `/malak` (مرة لكل جلسة عبر sessionStorage).
- `8680087` زر **[▶ استمع]** على كرت البريفينج.

### نطق الأرقام والصوت الطبيعي
- `bb4f137` **نطق الأرقام كلمات عربية** (ElevenLabs يخربط الأرقام المجرّدة) — مع حماية
  أكواد SKU مثل `mk1215` من التحويل.
- `df7550b`/`9b114e7` ضبط إعدادات الصوت لنبرة أبطأ وأطبع.

### رحلة الأصوات الخليجية (الأحدث)
- `de0fa5a` صوت مشترك واحد + كل وكيل يعرّف عن نفسه ("مرحبا، معاك [اسم] من قسم [وظيفة]").
- `ba7e1f8` استرجاع صوت لكل وكيل (راشد = الصوت الجديد).
- المشكلة: الأصوات طلعت **مصرية/AI/جنس خاطئ** مو خليجية.
- `14a3c99`/`9229d4b` تصفّح مكتبة ElevenLabs العربية + sampler مؤقت لتجربة الأصوات.
- `da3cac2` **تعيين أصوات خليجية معتمدة لكل وكيل، مطابقة للجنس** (بعد موافقتك على العينات):

  | الوكيل | الصوت | الوكيل | الصوت |
  |---|---|---|---|
  | راشد | Nasser (سعودي، ذكر) | نور | Salma (خليجي، أنثى) |
  | سراج | Ali (سعودي، ذكر) | بيان | Noura (خليجي، أنثى) |
  | سالم | Ali Ahmed (سعودي، ذكر) | ريم | Amira (خليجي، أنثى) |
  | | | رزان | Samira (خليجي، أنثى) |
  | | | لطيفة | Latifa (خليجي، أنثى) |

- `7a651ab`/`b8203c1` فحص نشر مؤقت للتأكد أن أصوات الخليج وصلت للموقع الحي، ثم تنظيفه.

### تنظيف وتوثيق (هذه الحصة)
- `6dafedc` تحديث تعليقات `route.ts` المضللة ("Phase 1 / read-only") لتعكس الواقع
  (قراءة + كتابة بتأكيد) — تنظيف تجميلي فقط بدون تغيير وظيفي.
- `29a017a` **سكربت تصدير طلبات** `export_talabat.mjs` (قراءة فقط، variants → صفوف).
- `77cc3fa` التصدير يأخذ الوصف الإنجليزي الناقص من الماستر شيت كاحتياطي.

---

## 🔐 نموذج الأمان (لم يُمسّ أبدًا)

1. **لا مسار كتابة يتجاوز التأكيد** — الكتابة فقط في `/commit` خلف توكن HMAC موقّع.
   `route.ts` و`/generate-image` و`/upload` لا تكتب في قاعدة البيانات (رفع للـStorage فقط).
2. توقيع **HMAC-SHA256** (السرّ = مفتاح الخدمة)، صلاحية **15 دقيقة**، مقارنة ثابتة الزمن.
3. **منع التنفيذ المزدوج** — `/commit` يرفض كتابة مطابقة خلال 30 ثانية.
4. الأسرار (مفاتيح API) تبقى على السيرفر فقط — المتصفح يشوف JSON النهائي بس.

---

## 📤 قسم Import / Export

الصفحة `/import-export` = بوابة دخول وخروج بيانات الكتالوج:

- **Upload Excel** — استيراد منتجات من إكسل (يُقرأ في المتصفح، معاينة، تأكيد، ثم كتابة).
  الجزء الوحيد في القسم اللي يكتب في قاعدة البيانات.
- **Image Upload** — رفع صور وربطها بمنتج.
- **Export per channel** — 4 أزرار تصدير CSV: Shopify / Snoonu / Talabat / Rafeeq
  (يسحب البيانات الحيّة، تنزّل الملف وترفعه يدويًا — ما يرفع تلقائيًا لأي منصة).
- **Snoonu Sync** — مطابقة ملف تصدير سنونو بالـ`snoonu_id` مع diff قبل التطبيق.

> ملاحظة: التصدير حاليًا Phase 1 (المخزون/الصور/البراند متروكة فاضية عمدًا).

---

## 🔎 اكتشافات مهمة حول البيانات (هذه الحصة)

### مصدر الكتالوج (Product Hub)
- **الملف المصدري:** `commerce-ai-os/Malikas_Universe_CLEAN_28col.xlsx` (1146 صف، 28 عمود، gitignored).
- **سكربت الاستيراد:** `scripts/import_products.mjs` (استيراد لمرة واحدة).

### مشكلة الـVariants والألوان
- عمود **Color** و**Size** في الماستر شيت **فاضيان تمامًا** (0 من 1146) — فالكتالوج
  ما عنده ألوان أصلًا من المصدر.
- معلومات الألوان موجودة فقط داخل عمود **Variant** لـ 64 منتج (نمط `"Blue | Pink"`)،
  محفوظة في جدول `product_variants.variant_name` (202 صف).
- سكربت الاستيراد يضع `product_variants.color = null` صراحةً (السطر 208).

### تصدير طلبات النهائي (`talabat_catalog_final.csv`)
- **1284 صف** = 1082 منتج بدون variants + 202 صف variant.
- صيغة SKU للـvariant: `{sku}-{رقم}` مثل `mk870-1`, `mk870-2`.
- اسم المنتج + " - " + اسم الـvariant.
- **115 منتج بدون وصف إنجليزي** — وتبيّن أن الماستر شيت نفسه فاضٍ لهم (مو مشكلة استيراد).

---

## 🚀 كيف تشغّل الأشياء

```bash
# المسار الأساسي
cd commerce-ai-os

# تثبيت الحزم
npm install

# تشغيل محلي
npm run dev            # ثم افتح http://localhost:3000/malak

# بناء للإنتاج
npm run build

# استيراد المنتجات من الماستر شيت (مرة واحدة)
node scripts/import_products.mjs

# تصدير كتالوج طلبات (قراءة فقط، يكتب talabat_catalog_final.csv)
node scripts/export_talabat.mjs
```

### النشر
- **Vercel** ينشر تلقائيًا عند `git push origin master` (~40 ثانية).
- الموقع الحي: `https://ghost-rho-two.vercel.app`

### متغيرات البيئة (أسماء فقط — تُحفظ في `.env.local` / Vercel)
```
ANTHROPIC_API_KEY
SUPABASE_SERVICE_ROLE_KEY          (+ يُستخدم كسرّ توقيع HMAC)
NEXT_PUBLIC_SUPABASE_URL
NEXT_PUBLIC_SUPABASE_ANON_KEY
ELEVENLABS_API_KEY
ELEVENLABS_VOICE_ID                (صوت ملاك الافتراضي)
ELEVENLABS_VOICE_<AGENT>           (اختياري — تجاوز صوت وكيل معيّن)
ELEVENLABS_MODEL_ID                (اختياري)
OPENAI_API_KEY                     (توليد الصور)
OPENAI_IMAGE_MODEL                 (اختياري، افتراضي gpt-image-1-mini)
MALAK_SIGNING_SECRET               (اختياري — بديل سرّ التوقيع)
```

---

## ✅ الحالة الحالية

- ملاك تعمل بالكامل: قراءة، كتابة بتأكيد، توليد صور/بوستات، بريفينج، صوت خليجي لكل وكيل.
- نموذج الأمان والتأكيد **سليم بالكامل** ولم يُمسّ في أي مرحلة.
- التصديرات تعمل (طلبات بالصيغة المطلوبة عبر السكربت).

## 📌 بنود مفتوحة (للمستقبل)

1. **115 منتج بدون وصف إنجليزي** — يحتاج مصدر أوصاف أو توليد بالـAI.
2. **ألوان الـVariants** — غير موجودة في المصدر؛ تحتاج إدخال يدوي أو ملف خارجي.
3. **تنظيف الصور اليتيمة** — صور مولّدة/مرفوعة غير معتمدة تبقى في الـbucket (خطة جاهزة، تنتظر الموافقة).
4. **تصديرات Phase 1** — المخزون والصور والبراند متروكة فاضية، تحتاج إكمال.
5. **بوستر Canva بنص عربي دقيق** — يحتاج Canva Enterprise (مكلف لمتجر واحد).
6. **تدوير المفاتيح المكشوفة** — عملية من طرف المستخدم.
