import MilestonePlaceholder from "@/components/MilestonePlaceholder";

export default function ChannelsPage() {
  return (
    <MilestonePlaceholder
      title="Channels"
      milestone="Arrives in M4"
      description="Product × channel matrix showing per-channel status (Active / Draft / Not Listed) with toggles, driven by the channel_products join table."
    />
  );
}
