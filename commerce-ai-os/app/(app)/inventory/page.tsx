import MilestonePlaceholder from "@/components/MilestonePlaceholder";

export default function InventoryPage() {
  return (
    <MilestonePlaceholder
      title="Inventory"
      milestone="Arrives in M4"
      description="Stock table with a low-stock filter and a sold-quantity column, backed by the inventory table as the single source of stock truth."
    />
  );
}
