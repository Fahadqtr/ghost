import MilestonePlaceholder from "@/components/MilestonePlaceholder";

export default function ProductsPage() {
  return (
    <MilestonePlaceholder
      title="Product Hub"
      milestone="Arrives in M4"
      description="Searchable product table plus add/edit form with all 28 master-sheet fields, category dropdown locked to the 11 categories, and a parent–child variant sub-form."
    />
  );
}
