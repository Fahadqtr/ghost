import MilestonePlaceholder from "@/components/MilestonePlaceholder";

export default function ImportExportPage() {
  return (
    <MilestonePlaceholder
      title="Import / Export"
      milestone="Arrives in M5"
      description="Upload Excel (.xlsx) with a preview mapped to the 28 columns, image upload to Supabase Storage, and per-channel export stubs (Shopify CSV, Snoonu masterlist, Talabat split-CSV, Rafeeq CSV)."
    />
  );
}
