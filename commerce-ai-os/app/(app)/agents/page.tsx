import MilestonePlaceholder from "@/components/MilestonePlaceholder";

export default function AgentsPage() {
  return (
    <MilestonePlaceholder
      title="Agents"
      milestone="Arrives in M4"
      description="A card per agent plus a command panel that writes a row to agent_logs. UI only — no AI logic and no external API calls in Phase 1."
    />
  );
}
