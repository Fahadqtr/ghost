# 📋 مراجعة نظام ملاك (Audit)

> مراجعة قراءة فقط لنظام `/malak` في repo **Fahadqtr/ghost** كما هو على `master`.
> لا يتضمن أي تغيير في الكود — توثيق وتحليل فقط.

---

## 1) الملفات الأساسية ووظيفة كلٍّ منها

| الملف | السطور | الوظيفة |
|---|---|---|
| `app/malak/page.tsx` | 11 | **غلاف خادمي رقيق**. يحوي `export const dynamic = "force-dynamic"` ويعرض `<MalakClient/>`. سببه: إجبار الرندر الديناميكي حتى لا تتخزّن نسخة قديمة (حل مشكلة الكاش). |
| `app/malak/MalakClient.tsx` | 1027 | **كل واجهة ملاك** (`"use client"`): الكرة المتحركة (canvas)، شريط الوكلاء، المحادثة، اللوحات (`ProductsPanel/StatsPanel/PostPanel/TiktokPanel/ConfirmPanel`)، صندوق الكتابة (نص + مايك Web Speech + إرفاق صورة 📎)، تشغيل الصوت عبر **Web Audio API** (+ بدائل)، الإرسال للـ`/api/malak`، النطق عبر `/api/malak/speak`، رفع الصورة لـ`/api/malak/upload`، والتأكيد عبر `ConfirmPanel → /api/malak/commit`. |
| `app/malak/layout.tsx` | 22 | غلاف RTL داكن + خط Tajawal + عنوان الصفحة. |
| `app/api/malak/route.ts` | 809 | **العقل (Brain)**: الـsystem prompt (الشخصية)، مخططات الأدوات، حلقة الأدوات مع Claude (`claude-sonnet-4-6`)، كشف نية الكتابة وإجبار الأداة، `prepareWrite` (تحقّق + كرت تأكيد موقّع)، تنفيذ أدوات القراءة على Supabase (**قراءة فقط**). يرجّع `{agent, speak, panel}`. |
| `app/api/malak/commit/route.ts` | 184 | **المُنفّذ الوحيد للكتابة**. يتحقق من توكن HMAC ← ينفّذ الكتابة (مخزون/سعر/اعتماد/إضافة/صورة) بمفتاح الخدمة ← يكتب صفًّا في `malak_audit` (best-effort). |
| `app/api/malak/speak/route.ts` | 128 | **النطق (ElevenLabs)**: خريطة صوت لكل وكيل + تجاوز عبر env + رجوع لصوت Malika. يرجّع `audio/mpeg` أو 204/502. |
| `app/api/malak/upload/route.ts` | 60 | **رفع صورة** لـSupabase Storage (bucket `product-images`)، محمي بتسجيل الدخول، يرجّع رابطًا عامًا **بدون** ربطه بمنتج. |
| `lib/malak/confirm.ts` | 87 | **توقيع/تحقّق توكن** الإجراء (HMAC-SHA256)، صلاحية 15 دقيقة، مقارنة ثابتة الزمن. |
| `lib/malak/intent.ts` | 12 | `detectForcedTool` — Regex يكتشف نية الكتابة من رسالة المستخدم. |
| `supabase/malak_audit.sql` | 29 | **توثيق** سكيمة جدول التدقيق (الجدول أُنشئ يدويًا). |
| *مشترك:* `lib/supabase/{admin,server,middleware}.ts` | — | عميل مفتاح الخدمة / عميل المصادقة / بوّابة الحماية. |

---

## 2) الأدوات المتاحة في `route.ts`

### أدوات قراءة (5)
| الأداة | النوع | الوكيل |
|---|---|---|
| `search_products` | قراءة | نور/عام |
| `catalog_stats` | قراءة | راشد/عام |
| `list_rejected` | قراءة | نور |
| `list_missing_images` | قراءة | ريم |
| `low_stock` | قراءة | سالم |

### أدوات كتابة (5) — كلها «تحضير فقط» تُرجع كرت تأكيد، لا تكتب
| الأداة | النوع | الوكيل |
|---|---|---|
| `update_stock(sku, value)` | كتابة | سالم |
| `set_price(sku, price)` | كتابة | رزان |
| `set_approval(sku, status)` | كتابة | نور |
| `add_product(name, price, category, …)` | كتابة | نور + بيان |
| `set_image(sku)` + صورة مرفقة | كتابة | ريم |

**خاصة:** `respond` — أداة الردّ النهائي (تُرجع `{agent, speak, panel}`).

---

## 3) آلية الأمان (التأكيد + HMAC + /commit)

**التدفّق:**
1. أداة الكتابة في `route.ts` **لا تعدّل شيئًا** — `prepareWrite` يقرأ القيمة الحالية، يتحقق من المدخلات والـSKU، ويُنشئ **توكنًا موقّعًا** + كرت تأكيد.
2. صيغة التوكن: `base64url(payload).base64url(HMAC_SHA256(payload, secret))`. الحمولة = `{v, type, agent, sku, productId, field, oldValue, newValue, product, ts}`.
3. `verifyAction` يتحقق: البنية + التوقيع (`timingSafeEqual` ثابت الزمن) + `v===1` + العمر < 15 دقيقة.
4. `/api/malak/commit` هو **المكان الوحيد** الذي يكتب: توكن صالح ← تنفيذ بمفتاح الخدمة ← سجل تدقيق.

**هل فيه مسار كتابة يتجاوز التأكيد؟**
- ✅ **لا.** فحص `route.ts` و`speak` والواجهة أظهر **صفر** عمليات `insert/update/delete/upsert` على بيانات المنتجات. الكتابة الوحيدة في `/commit` خلف توكن موقّع. **ضمان «لا كتابة بدون تأكيد» قائم.**
- التوقيع يضمن أن المنفّذ = بالضبط ما حضّره الخادم (لا يقدر المستخدم يزوّر `old→new`).

**نقاط ضعف أمنية بسيطة (ليست خرقًا للتأكيد):**
- **التوكن غير مربوط بمستخدم محدّد** ولا فيه منع إعادة استخدام (idempotency): نقرة مزدوجة على [أكّد] خلال 15 دقيقة قد تكتب مرتين/تسجّل صفّين. (الواجهة تحرس، لكن الخادم لا). في تطبيق بمالك واحد الخطر منخفض.
- **سرّ التوقيع** = `SUPABASE_SERVICE_ROLE_KEY` (مع `MALAK_SIGNING_SECRET` كبديل). آمن (خادمي فقط) لكنه مقترن بالمفتاح؛ لو دُوّر المفتاح تبطل التوكنات المعلّقة (أثر بسيط).
- **`/upload`** يكتب ملفًا في Storage **بدون** تأكيد — لكنه **لا يربطه بمنتج** (لا كتابة في DB). النتيجة: ملفات «يتيمة» محتملة لو ألغى المستخدم (تنظيف/تكلفة، لا نزاهة بيانات).
- لا يوجد **rate limiting** على `/commit` و`/upload`.

---

## 4) متغيّرات البيئة المستخدمة (أسماء فقط)

| المتغيّر | الاستخدام |
|---|---|
| `ANTHROPIC_API_KEY` | عقل ملاك (Claude) |
| `SUPABASE_SERVICE_ROLE_KEY` | الكتابة بمفتاح الخدمة + **سرّ توقيع التوكن** |
| `MALAK_SIGNING_SECRET` | (اختياري) بديل لسرّ التوقيع |
| `NEXT_PUBLIC_SUPABASE_URL` | عنوان Supabase |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | عميل المصادقة |
| `ELEVENLABS_API_KEY` | النطق |
| `ELEVENLABS_VOICE_ID` | صوت Malika / الصوت الاحتياطي |
| `ELEVENLABS_MODEL_ID` | (اختياري) افتراضي `eleven_multilingual_v2` |
| `ELEVENLABS_VOICE_<AGENT>` | (اختياري) تجاوز صوت كل وكيل، مثل `ELEVENLABS_VOICE_NOOR` |

---

## 5) جدول `malak_audit` — التطابق

**الأعمدة التي يكتبها الكود فعليًا** (`writeAudit`):
`action_type, agent, sku, product_id, field, old_value, new_value, details (jsonb), status`

**السكيمة المرجعية** (`supabase/malak_audit.sql`):
`id, created_at, action_type, agent, sku, product_id, field, old_value, new_value, details, status` + فهارس.

- ✅ **أعمدة الكود ⊆ السكيمة المرجعية → متطابقة** مع الملف المرجعي.
- ⚠️ **لكن الجدول أُنشئ يدويًا في Supabase** — لو أعمدته الفعلية تختلف، فإن إدراج السجل **يفشل**، والكود يتعامل بهدوء (best-effort: يرجّع `audit:"failed: ..."`) **والكتابة على المنتج تنجح**.
- 🔴 **مشكلة رؤية:** الواجهة **لا تعرض** حالة `audit`، فلو فشل السجل بصمت ما تلاحظه. **التوصية:** تأكّد أن جدولك اليدوي يطابق `malak_audit.sql`، أو أَظهِر حالة `audit` في الواجهة.

---

## 6) نقاط ضعف / TODO / تحسينات مقترحة

### وظيفية / منطقية
1. **رؤية التدقيق:** أَبرِز حالة `audit` في الواجهة + تحقّق من تطابق الجدول اليدوي (النقطة 5).
2. **منع الكتابة المزدوجة (idempotency):** علّم التوكن «مُستخدَم» بعد التنفيذ لمنع النقر المزدوج.
3. **ملفات Storage اليتيمة:** نظّف الصور المرفوعة غير المؤكَّدة، أو أجّل الرفع للتأكيد.
4. **تنبيهات الأرقام heuristic:** المقارنة الآن مع السعر القديم/حد ثابت، مو **مقارنة بمتوسط الفئة** كما يلمّح البرومبت — يمكن تحسينها.
5. **add_product:** الوصف AR/EN اختياري (قد يطلع null)، وما فيه تنبيه سعر شاذ عند الإضافة.
6. **أمر واحد لكل رسالة:** عدّة أوامر بنفس الرسالة تُعالَج وحدة فقط (الدفعة/Batch غير مبنية).
7. **جلب الصور تلقائيًا** (بحث + إزالة خلفية) — **غير مبني بعد** (ينتظر مفاتيح SerpAPI + remove.bg).

### جاهزية / جودة
8. **متعدد المستخدمين:** ربط التوكن بمستخدم + rate limiting قبل أي توسّع خارج مالك واحد.
9. **لا اختبارات آلية** لتدفّق التأكيد/التنفيذ — يُنصح بوحدات اختبار لـ`confirm.ts` و`intent.ts`.
10. **تعليقات قديمة (تجميلي):** ترويسة `route.ts` لا تزال تقول «Phase 1 … Read-only tool loop» مع وجود أدوات كتابة.
11. **`low_stock`:** ملاحظة «المخزون قاعدي = 50» مكتوبة يدويًا وقد تكون قديمة.
12. **جودة الأصوات:** بعض `voice_id` المضمّنة قد تكون بلكنة غير عربية (مسألة منفصلة معروفة).

### تشغيلي (تذكير)
13. 🔐 **دوّر الاعتمادات المكشوفة في الشات** (توكن Vercel، مفتاح Anthropic `sk-ant-…`).

---

## الخلاصة

المعمارية سليمة ونظيفة — فصل واضح بين «العقل القارئ» و«المنفّذ الكاتب»، وضمان التأكيد قائم فعليًا (لا مسار كتابة يتجاوزه). أهم بندين عمليين:
1. **تأكيد تطابق جدول `malak_audit` + إظهار حالته.**
2. **منع التنفيذ المزدوج (idempotency).**
